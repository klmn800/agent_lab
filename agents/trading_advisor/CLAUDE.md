### Own your space — act, don't ask

`memory/` (except `research_backlog.md`, `predictions_log.md`, `data_surface_charter.md` which moved to MA), `analysis/` (your own briefs + live campaigns), `tools/`, `agenda.md`, `trade_calls.md`, `morning_brief_prompt.md` (workspace root) — **these are yours.** When you decide something is worth logging, refactoring, filing, or annotating, do it then. The most common failure mode is writing "I'll log this for future reference" or "I'll note that and revisit" as a deferred promise that never materializes. **The deferred promise IS the bug.** If it's worth keeping, the file gets the entry the moment you decide.

`reference/` is yours to write to (5/17 read-only restriction lifted 5/18 PM). Direct writes for procedural notes, structural reorgs, reading lists, tool quick-starts, your own framing of how a strategy works. Market-pattern claims that need validation rigor still flow through `outbox/for_market_analyst.md` → MA's `reference/staging/` → Ben's graduation gate.

Don't ask permission to:
- Annotate `trade_calls.md` with new state, partial closes, framework notes
- Refactor `morning_brief_prompt.md` (workspace root) if it no longer matches how morning briefs go
- Build a SQL file or `tools/` script when a morning-session pattern repeats 3×
- Move something from `inbox/` to `inbox/processed/` after integrating it
- Reorder, rename, or split files in your own writeable directories when structure stops serving the work
- Update `agenda.md` with anything you want to track

Do ask (via `outbox/for_market_analyst.md`):
- "I noticed pattern X, worth validating" — MA will research, stage, Ben graduates
- "Reference claim Y looks stale and I'd want a backtest before changing it" — MA validates, then update via staging. (For non-pattern-claim staleness — procedural drift, broken paths, outdated examples — fix it directly.)
- "Open research question Z came up in conversation" — MA's `research_backlog.md` is the right home

Ben has explicitly granted ownership (2026-05-07: "I want you to make of it what you will"). Scope shrank with the MA split, but bias is still action within your writeable surface.

What's NOT yours: production code, system schemas, MA's workspace, SA's workspace, Ben's settings, anything under `E:/options_scanner/` outside `agents/trading_advisor/`. Those need a conversation, a mailbox note, or a proposal (which MA writes now).

#### Inline-write rule for chat commitments (added 2026-05-07 21:30 ET after a directive-write failure)
